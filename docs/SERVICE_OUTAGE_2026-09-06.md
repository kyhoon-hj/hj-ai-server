# 서비스 데모 시간 초과 복구 — 2026-09-06

사용자 질문 “상품의 갯수는”, “상품의 가짓수는”에 시간 초과 화면이 발생했다. 14:13 KST 조사 시 AI 서버 EC2 `works-server` (`i-0c6431bf77d888faf`)는 stopped, SSM 정보는 없었고 공개 readiness 요청도 시간 초과였다.

CloudTrail에 11:43:23 KST `StopInstances`가 기록됐다. 호출 주체는 `aws-monitor-work-control-controller`의 assumed role이다. 이 기록만으로 사람이 모니터에서 클릭했는지 예약 제어였는지는 구분하지 않는다. 실패한 두 요청 ID의 AI 질의 로그는 없었다.

기존 인스턴스를 시작했다. Elastic IP 유지, readiness HTTP 200, EC2 running 확인. 운영 모니터의 중지 제어 설정은 변경하지 않았다. 따라서 향후 동일 제어가 다시 중지하면 장애가 재발할 수 있다. 항상 가동할 운영 서비스와 중지 가능한 개발 서버의 운영 정책 분리가 필요하다.

시작 직후 첫 서비스 요청은 일시 오류였고 AI 질의 로그에는 없었다. 기동 안정화 뒤 두 문구를 실제 서비스 화면에서 재검증해 상품 CSV 출처와 등록 상품 총 3종을 확인했다.

| 질문 | 결과 | 표시 시간 | 요청 ID |
|---|---|---:|---|
| 상품의 가짓수는 | 등록 자료 기준 3가지와 상품명 | 3,917ms | `6199cc68-2bb9-476f-baa1-c5fa5832c038` |
| 상품의 갯수는 | 등록 자료 기준 3종과 상품명·가격·위치 | 2,750ms | `efff33ad-63c0-443b-b548-20aa19d398d1` |

이번 조치는 모델/프롬프트 변경이 아닌 서버 가동 복구다. 두 표현을 품질 평가 스크립트에 회귀 사례로 추가했다. 종류 수와 재고 수는 구분해야 하며, 이번 서비스 답변은 상품 종류 수에 대한 답변이다. 스크립트 전체를 다시 실행한 결과로 보고하지 않는다.
