# Service Demo

마트 고객응대와 이후 매출 분석을 위한 서비스 데모 작업 공간입니다. 실행 애플리케이션은 `ENV-SVC-01`에서 구현합니다.

포트는 환경과 무관하게 다음 값으로 고정합니다.

- 서비스 데모: `11002`
- AI Server 연결 기본값: `http://127.0.0.1:11000`

`SERVICE_DEMO_PORT`에 11002 외 값을 설정하면 `port.config.mjs`가 시작 설정을 거절합니다.
