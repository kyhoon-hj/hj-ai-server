# HJ AI Server 프로젝트 작업 지침

## Console 마일스톤 추적

- Console 개발·수정·검증 작업을 시작하기 전에 `docs/HJ_AI_CONSOLE_MILESTONES.md`의 현재 단계, 선행 조건, 완료 기준을 확인한다. 다른 영역의 작업에는 이 절차를 적용하지 않는다.
- 시작 안내와 최종 보고에 `[Console M번호 | 단계명 | 상태 | 완료 작업/전체 작업]`을 표시한다. 장시간 작업의 중간 안내에도 현재 단계와 수행 중인 작업 ID를 포함한다.
- 구현에 실제 착수할 때만 `진행 중`, 구현 후 완료 기준을 확인할 때는 `검증 중`으로 변경한다. 계획 작성만으로 구현 작업을 진행 또는 완료 처리하지 않는다.
- 단계 상태의 기준 문서는 `docs/HJ_AI_CONSOLE_MILESTONES.md`다. 작업 종료·단계 전환 시 현재 단계, 단계 표, 상세 체크리스트, 검증 근거, 다음 작업과 변경 이력을 함께 갱신한다.
- 체크리스트와 단계 완료 기준을 모두 충족한 경우에만 `완료`로 표시한다. 미실행 검증, mock 검증과 실제 DB·브라우저·운영 검증을 구분하고 실행 명령·날짜·결과·제약을 기록한다.
- 범위나 상태가 달라지면 기존 진행 현황과 구축 계획의 관련 항목도 동기화한다. 과거 검증 기록을 최신 검증으로 덮어쓰지 않는다.
- 외부 의존성으로 진행할 수 없으면 `차단`으로 표시하고 사유·해소 조건·가능한 독립 작업을 기록한다. 단순 미착수는 `대기`다.
- 마일스톤 문서 작성은 전체 구현·다른 프로젝트 변경·운영 배포 실행의 승인이 아니다. 현재 사용자가 요청한 작업 범위 안에서 진행한다.
